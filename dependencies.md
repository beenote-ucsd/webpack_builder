# Dependency

Run

```shell
npm install -g webpack
npm install --save-dev  babel-loader babel-core babel-preset-es2015 \
                        babel-preset-stage-0  babel-preset-react  json-loader \
                        file-loader  html-loader sass-loader url-loader
```
